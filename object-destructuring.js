const student = {
    name : "kalpesh",
    surname : "dhola",
    ege : 25
}

// const {ege} = student;  // this way to call object inner values 

// const {ege: e} = student;  // this way to rename object re calling 

console.log(e);
